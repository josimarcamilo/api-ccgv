meta {
  name: create conta interna
  type: http
  seq: 2
}

post {
  url: {{host}}/accounts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "Name": "banco do brasil - Lar dos velhinhos",
    "UnidadeId": 4
  }
}
